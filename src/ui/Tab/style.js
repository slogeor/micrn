import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  tab: {
    height: 30,
    flexDirection: 'row',
    alignItems: 'stretch',
    justifyContent: 'space-between',
  },
});
